# Private / internal — never publish
_private/
_chat.txt
CLAUDE.md

# OS / editor cruft
.DS_Store
Thumbs.db
*.log

# staging dump (raw WhatsApp media) — not for the site
more tennis/

# raw WhatsApp dumps (root) — not for the site
WhatsApp Image*
